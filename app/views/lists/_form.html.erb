<%= form_with model: list do |f| %>
  <div class="lineitem">
    <p><%= f.label :title, "New list" %></p>
    <%= f.text_field :title, autofocus: true %>
    <%= f.submit "Create" %>
  </div>
<% end %>
