<!DOCTYPE html>
<html>
  <head>
    <title>Finalist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="turbo-prefetch" content="false">


    <%= stylesheet_link_tag "all.min", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <!-- Optional: if you copy your icon files into /public, these paths will work as-is -->
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#5bbad5">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
  </head>

  <body>
    <% unless content_for?(:nav_title) %>
      <% content_for :nav_title, (@list&.title || "Finalist") %>
    <% end %>
    <%= render "shared/navbar" %>

    <% if flash[:alert].present? %>
      <div class="errormsg"><p><%= flash[:alert] %></p></div>
    <% end %>

    <main id="main">
      <%= yield %>
    </main>
  </body>
</html>
